/* virtual_bank.h */

#pragma once

#include <iostream>
#include <string>
#include <vector>
#include "../shared/vbank.h"

std::vector<std::string> split(const std::string &input, const std::string &delimiter);
